# M0 test harness (placeholder)

Mazen's M0 isolation / ping-restart / exit harness is **not** part of M0-IN-01.

CI jobs `m0-isolation`, `m0-ping-restart`, and `m0-exit` are defined as placeholders and
skip when harness files are absent under `tests/m0/`.

Do not add harness implementation in this directory during IN-01 bootstrap.
